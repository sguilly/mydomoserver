CREATE TABLE SENSORVALUE (ID INTEGER NOT NULL, DATE TIMESTAMP, IDSENSOR VARCHAR(255), TYPE INTEGER, VALUE REAL, PRIMARY KEY (ID))
CREATE TABLE NAMESENSOR (ID INTEGER NOT NULL, DATE TIMESTAMP, IDSENSOR VARCHAR(255), NAME VARCHAR(255), TYPE VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE SENSOREVENT (ID INTEGER NOT NULL, DATE TIMESTAMP, IDSENSOR VARCHAR(255), VALUE VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
